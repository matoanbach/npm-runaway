apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: datatrace
  region: us-east-2

nodeGroups:
  - name: datatrace
    instanceType: t3.small
    desiredCapacity: 1
    volumeSize: 10
